# SourceBox 更新日志

## 1.0.6.3
* 修复 `VPK编辑器` 在一键导出时会导出当前包中不存在文件的问题
* 改进创意工坊资源读取策略

## 1.0.6.2
* 改进 `创意工坊管理` 功能，新增创意工坊合集管理/订阅，通过合集查询当前订阅状态
* 新增 `CS2 GSI` 功能，用于 `OBS直播插件` 实时显示当前玩家状态. 并且支持自定义部分事件音效
* 新增 `VPK复用素材压缩` 设置，启用后在打包时对相同素材进行压缩 (实验性功能)
* 修复部分弹出框在未激活窗口时不渲染问题
* 修复部分旧版本 Windows(21H2之前) 软件闪退问题
* 部分组件性能问题优化/重写

## 1.0.6.0 - 1.0.6.1
> 感谢大家在过去一年的陪伴与支持！祝大家新年快乐，愿新的一年里心想事成，万事顺意！
* 重写 `创意工坊管理` 功能，支持创意工坊订阅和发布管理
* 重构 `窗口、对话框` 样式，改用 Win32API 绘制提高窗口渲染性能
* 重构 `状态栏` 样式，统一状态栏显示样式
* 改进 `设置` 功能，支持软件配置路径迁移
* 改进 `MOD管理` 删除行为，删除后自动收录至回收站方便找回
* 改进 `VPK 打包` 功能，封面如果超出 512x512 尺寸将自动压缩尺寸
* 改进 `VPK编辑器` 功能，资源一键导出时支持选择部分文件导出
* 优化 `MOD管理` 封面渲染方式，改为后台渲染并限制为 512x512 大小. 节省内存开销
* 修复 `服务器列表` 因服务器信息查询失败导致地图封面仍然读取问题
* 修复 `创意工坊管理` 因配置丢失导致初始化失败问题
* 优化组件加载方式加速软件启动速度
* 规范部分控件代码，减少软件打包体积
* 部分请求错误降级非严重信息，防止频繁错误提示
* 部分配置不再通过保存本地文件进行保存，改用注册表方式保存. 防止引权限问题读取错误

## 1.0.5.0
* 软件运行库升级为 .NET 10 LTSC
* 删除 `软件关于` 界面
* 新增 `关于\设置` 界面，主要用于设置主题和部分数据清理
* 改进 `服务器列表` 功能，详细信息展开时显示当前服务器地图封面
* 改进 `服务器列表` 功能，支持记忆当前选择服务器类型
* 改进 `模组管理` 功能，支持批量拖拽 .vpkmod 文件来添加模组
* 改进 `VPK编辑器` 功能，支持在编辑器内套娃打开 VPK 文件
* 改进 `资源编辑` 功能，支持预览 vcss，vxml，vtex(仅支持部分主流格式材质) 预览
* 改进 `创意工坊文件管理` 功能，根据对应创意工坊文件显示封面作为背景
* 优化 `VPK编辑器` 重命名编辑时渲染性能
* 优化 `资源编辑` 读取性能，并优化读取时内存占用
* 优化 `创意工坊文件管理` 判断地图数量方式，改为根据地图碰撞来判断地图数量
* 修复 `资源编辑` 部分资源资源数据预览时，因属性空值导致报错问题
* 修复 `VPK编辑器` 部分VPK包不计算大小问题
* 修复 `VPK编辑器` 修复文件在删除后 添加文件/重命名 后，打包 .vpkmod 文件报错问题
* 修复 `STEAMID查询` 使用 SteamID3 查询信息时，转换后信息错误问题

## 1.0.4.1
* 修复 `VPK编辑器` 添加文件夹内容时，文件夹内包含冲突文件导致无法打包 VPKMod 问题

## 1.0.4.0
* 改进 `服务器列表` 功能，支持用户自己订阅服务器列表 (由于本人无法长期进行维护服务器列表)
* 改进 `VPK编辑器` 功能，支持批量文件重命名、相同路径复制为副本
* 修复 `VPK编辑器` 复制粘贴时出现异常问题
* 修复 `VPK编辑器` 部分快捷键功能失效问题
* 修复 `VPK编辑器` 部分文件进行移动复制后，路径结构无法保存问题
* 修复 `VPK编辑器` 内执行文件操作后，无法导出为 VPKMod 问题
* 修复 `资源编辑` 打开部分音频资源文件报错问题

## 1.0.3.10
* 修复 `资源编辑` 部分版本资源读取失败问题
* 修复 `资源编辑` 对资源解析后部分属性数值类型错误问题
* 修复 `创意工坊管理` 删除地图后保存配置时因根节点问题保存失败问题
* 重写部分组件渲染方式，优化界面性能

## 1.0.3.9
* 改进 `自动挤服` 功能，自动挤服运行时禁止修改人数和延迟
* 修复 `VPK编辑器` 里进行资源编辑时，未及时关闭资源编辑操作界面导致报错问题
* 修复 `资源管理` 部分资源排序异常问题

## 1.0.3.7 - 1.0.3.8
* 优化 `模组管理` 加载过多Mod导致内存暴涨以及界面卡顿问题
* 修复 `模组管理` 部分正常模组不显示问题

## 1.0.3.6
* 删除 `错误日志` 自动写出到 logs 功能
* 优化 `资源编辑` 文本字体样式，增强可读性
* 优化 `资源编辑` 读取到未知类型文件时，直接采用16进制编辑器方式显示
* 补全 `VPK编辑器` 部分资源文件图标
* 修复 `资源编辑` 时解析部分类型报错问题
* 修复 `资源编辑` 读取特定类型文件报错问题
* 修复窗口部分字体样式失效问题

## 1.0.3.5
* 改进 `资源编辑` 功能，支持显示当前资源引用的所有相关文件
* 修复 `资源编辑` 部分非 kv3 结构仍然可以保存问题
* 优化 `VPK编辑器` 导出相关资源性能
* 修复 `VPK编辑器` 读取部分文本内容报错问题

## 1.0.3.4
* 修复 `VPK编辑器` 新建文件保存后刷新目录报错问题
* 修复 `资源编辑` 因 AnimGraph2 系统更新导致部分资源读取失效问题
* 改进 `动画帧生成` 功能，新建时动画自动重置动画时间帧

## 1.0.3.3
* 新增 `资源编译` 功能，支持资源进行手动编译
* 优化部分页面加载卡顿问题

## 1.0.3.2
* 改进 `动画帧生成` 功能，支持通过拖拽形式改变顺序位置
* 改进 `动画帧生成` 功能，支持预览当前编辑帧动画
* 优化 `动画帧生成` 网格划分计算方式确保内容填充
* 修复 `动画帧生成` 重复导入操作时，相同动画帧不渲染问题
* 修复 `OBS直播工具` 关闭后无法正常保存问题

## 1.0.3.1
* 新增 `动画帧生成` 功能，适用于多个图片或GIF转为 序列图片/文件
* 修复 `OBS直播工具` 初始化时有概率导致配置丢失问题
* 修复一些主题字体和字体颜色相关问题

## 1.0.3.0
* 新增 `VPK 编辑器` 导出当前选中资源所有相关文件
* 优化 `OBS 直播工具` 性能，并简化配置不再需要占用多个端口
* 修复 `资源编辑` 里选择数据块时出现性能问题
* 修复 `资源编辑` 读取部分类型资源文件数据块失败问题
* 修复 `资源编辑` 读取部分未进行压缩资源导致读取异常问题

## 1.0.2.7 - 1.0.2.8
* 修改 `服务器信息` 获取列表接口
* 修复 `窗口` 拖拽时会出现异常问题
* 修复 `资源编辑` 高级模式文本编辑下，处理多行文本值异常问题
* 修复 `资源编辑` 高级模式文本编辑下，编译时处理单行数组值异常问题
* 修复直接保存 VPKMOD 报错问题
* 移除对旧版 VPKMOD 的支持，不再自动转换成新版

## 1.0.2.3 - 1.0.2.6
* 修复 `资源编辑` 删除部分项导致报错问题
* 修复 `VPK 编辑器` 重命名操作后跳转其他页问题
* 修复 `MOD信息` 边框渲染问题
* 修复 `进度条` 报错导致进度丢失问题
* 改进 `MOD管理` 在安装模组结束时自动清理以前残留分卷文件
* 优化 `VPK编辑` 加载卡顿问题
* 优化 `MOD管理` 在卸载 Mod 时卡顿问题

## 1.0.2.1 - 1.0.2.2
* 改进 `服务器列表` 加载其他社区服列表失败时，添加手动
* 改进 `VPK资源编辑` 功能，支持双击资源文件进行编辑
* 删除 `资源编辑` 实验性功能，改为拖拽资源文件进行编辑
* 修复 `VPK 编辑器` 进行复制粘贴操作时，文件粘贴至错误目录问题
* 修复 `OBS 直播工具` 设置为 启用 时，软件启动后不会自启服务问题
* 修复 `资源编辑` 编译时因部分属性数值负值导致编译失败问题
* 修复 `快速挤服` 和 `添加服务器` 无法打开问题
* 修复正在加载时进行其他操作导致界面渲染出现错误问题

## 1.0.2.0
* 新增 `资源编辑` 用于快速编辑属性，不通过官方工具进行重新编译 (实验性功能)
* 修复 `窗口界面` 毛边渲染异常和最大化不完全问题
* 修复 `资源管理` 读取部分配置时失败问题
* 修复 `OBS直播工具` 频繁切换导致软件闪退问题
* 修复 `创意工坊管理` 部分地图计算数量错误问题
* 修复 `创意工坊管理` 部分分卷包命名问题导致清理时意外被删除
* 优化 `VPK 打包界面` 图片上传组件渲染性能
* 优化 `标签页` 内容展开过多时显示滚动按钮

## 1.0.1.1
* 添加 `VPK 管理` 里添加旧版 Mod 时，自动转换为新版 Mod
* 修复 `资源管理` 因文件名编码导致读取失败问题
* 修复 `Mod 安装` 打包后资源异常问题

## 1.0.1.0
* 添加 `主题切换`
* 修复 `部分组件` 颜色异常问题
* 修复 `部分输入框` 文本内容超出不滚动问题
* 修复 `资源管理` 部分创意工坊文件无法被扫描问题
* 修复 `资源管理` 因Steam客户端缓存策略修改无法获取应用图标问题
* 重绘 `VPK 管理界面` 界面，支持显示当前 Mod 预览封面和其他安装信息
* 重绘 `VPK 打包界面` 界面，支持绑定当前 Steam 用户名
* 重写 `Mod安装包` 打包方式，更新后支持压缩并且能安装多个创意工坊. 并且以前的 Mod 安装包不再支持提供安装
* 重写 `VPK 编辑器` 缓存策略，并优化读写性能
* 优化 `窗口` 调节框判定问题

## 1.0.0.6
* 变更 `检测更新` 检测方式，新增重新检测按钮. 防止因网络问题导致检测版本时间过长，用户无法感知到当前应用是否已经启用问题

## 1.0.0.5
* 修复 `VPK 编辑器` 直接保存过程中出现错误后导致数据结构丢失问题
* 修复 `VPK 编辑器` VpkMod 无法直接保存问题
* 修复 `Mod安装` 安装后重复记录已安装问题

## 1.0.0.4
* 修复 `Mod文件管理` 扫描到不相关内容
* 修复 `Mod安装` 因文件占用时导致安装失败，依然返回提示安装完成状态问题
* 优化 `Mod安装` 安装多个Mod性能
* 优化 `Mod安装` 安装时显示信息，因队列安装方式无法正确显示出当前任务进程问题

## 1.0.0.3
* 修复 `Mod安装` 因备份文件出现异常情况，导致原资源文件损坏问题
* 修复 `Mod安装` 因文件占用时导致安装失败，依然返回提示安装完成状态问题

## 1.0.0.2
* 新增 `当前用户` 如果为非管理员模式显示 `用户模式` 标识，如果用户安装将提示是否拥有管理员权限
* 修复 `Mod添加` 因文件冲突无法正确显示内容导致闪退问题
* 修复 `Mod安装` 因其他打包方式造成安装失败问题
* 修复 `VPK 编辑器` 因其他打包方式造成文件大小计算错误问题
* 调整 `VPK 编辑器` 移除VPK文件 2GB 限制，如果超出 2GB 则采用其他保存策略
* 修复 `Mod文件管理` 因未加载完毕地图数据切换标签导致软件崩溃
* 优化 `OBS 直播插件` 部分性能问题

## 1.0.0.1
* 添加 `国外服务器 IP`
* 修复 `服务器列表` 因查询国外服务器时目标服务器延迟过高无法查询到问题
* 修复 因修改安装策略导致配置保存在错误路径问题
